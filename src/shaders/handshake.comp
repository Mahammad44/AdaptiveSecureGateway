#version 450
layout(local_size_x = 64) in; // 64 threads per workgroup

// This matches your Rust buffer
layout(std430, binding = 0) buffer DataBuffer {
    uint data[];
};

void main() {
    uint gID = gl_GlobalInvocationID.x;
    data[gID] = data[gID] * 2; 
}
